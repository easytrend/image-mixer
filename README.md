# Image Mixer

A single-file, client-side image mixing tool. Upload multiple images as layers, position/resize/rotate, adjust opacity & blend mode, and export the result as PNG or JPG.

## Run locally

- Open `index.html` in your browser.

## Deploy (GitHub Pages)

This repo is designed to work from the repository root on GitHub Pages (Pages looks for `index.html`).

